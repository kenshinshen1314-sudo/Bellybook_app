## Codebase Patterns

(Patterns discovered during implementation will be added here)

---

# Progress Log

## [2025-01-13] Prep Phase Complete
- Created prd.json with 24 user stories
- Initialized progress tracking
- Ready to begin execution

---

## [2025-01-13 19:30] - US-001: 添加PWA Manifest配置
**Implemented:**
- Created /public/manifest.json with PWA configuration
- Added manifest link and PWA meta tags to index.html
- Created SVG icon as placeholder (icon.svg)
- Configured standalone display mode
- Build test passed

**Files Changed:**
- index.html - Added manifest link, PWA meta tags (theme-color, apple-mobile-web-app-capable, etc.)
- /public/manifest.json - New file with complete PWA configuration
- /public/icon.svg - New SVG icon placeholder

**Learnings:**
- PWA manifest requires display: "standalone" for app-like experience
- Apple-specific meta tags needed for iOS home screen support
- Multiple icon sizes defined in manifest (72x72 to 512x512)
- Theme color (#10B981) matches vibe design system primary color
- Build successful with manifest properly included

**Gotchas:**
- PNG icons still needed (SVG created as placeholder)
- iOS requires apple-touch-icon link for home screen icon
- Need to test on actual mobile device for "Add to Home Screen" prompt

---

## [2025-01-13 19:35] - US-002: 创建Service Worker基础框架
**Implemented:**
- Created /public/sw.js with complete Service Worker implementation
- Implemented install event (caches static assets)
- Implemented fetch event (network-first strategy with cache fallback)
- Implemented activate event (cleans up old caches)
- Added SW registration in src/index.tsx with update mechanism
- Build test passed, SW copied to dist

**Files Changed:**
- /public/sw.js - New file with complete SW implementation
- src/index.tsx - Added registerServiceWorker() function and update handling

**Learnings:**
- Network-first strategy for HTML/API ensures fresh content
- Cache-first for static assets (CSS/JS/images) improves performance
- SW update mechanism: updatefound → installed → controllerchange
- skipWaiting() activates new SW immediately without waiting for all tabs to close
- Build assets have hash in filename (index-DiEeViGh.js) - need to update PRECACHE_URLS after each build

**Gotchas:**
- Service Worker only works on HTTPS or localhost
- sw.js must be at root level to control entire app
- Need to update PRECACHE_URLS after each build (hardcoded currently)
- Consider using vite-plugin-pwa (US-003) to automate cache management

---

## [2025-01-13 19:40] - US-003: 配置Vite PWA插件
**Implemented:**
- Installed vite-plugin-pwa and workbox-window dependencies
- Configured VitePWA plugin in vite.config.ts with autoUpdate mode
- Set up workbox runtime caching (NetworkFirst for API, CacheFirst for images)
- Configured manual code splitting (react-vendor, ui-vendor chunks)
- Updated src/index.tsx to use virtual:pwa-register
- Build generates automatic SW with proper precaching

**Files Changed:**
- vite.config.ts - Added VitePWA plugin configuration with workbox caching
- src/index.tsx - Replaced manual SW registration with virtual:pwa-register
- package.json - Added vite-plugin-pwa, workbox-window dependencies

**Learnings:**
- vite-plugin-pwa auto-generates sw.js and workbox files during build
- virtual:pwa-register provides cleaner API with onNeedRefresh/onOfflineReady callbacks
- autoUpdate mode automatically detects and prompts for new versions
- Code splitting reduces bundle sizes: react-vendor (124KB), ui-vendor (357KB), index (317KB)
- workbox-window provides update UI components (can be customized)

**Gotchas:**
- Icons referenced in manifest must actually exist (will need PNG conversion from SVG)
- devOptions.enabled: true allows SW testing in development mode
- registerSW returns a function to manually trigger updates
- PWA works best with HTTPS, localhost has some limitations

---

## [2025-01-13 19:45] - US-004: 实现离线检测和UI提示
**Implemented:**
- Created src/hooks/useOnline.ts hook for network status detection
- Created OfflineBanner component with amber warning banner
- Integrated offline detection in App.tsx
- Added offline check in handleCameraClick (disables photo analysis when offline)
- Offline banner animates in/out using framer-motion spring animations

**Files Changed:**
- src/hooks/useOnline.ts - New hook using navigator.onLine and online/offline events
- src/components/OfflineBanner.tsx - New component with WifiOff icon and animation
- src/App.tsx - Added useOnline hook, OfflineBanner, and offline check in handleCameraClick

**Learnings:**
- navigator.onLine provides initial online status
- online/offline events trigger automatically when network status changes
- useOnline hook uses useState and useEffect for reactive updates
- Offline banner uses fixed positioning with z-[100] to appear above all content
- Spring animation (stiffness 300, damping 30) provides smooth slide-in effect

**Gotchas:**
- Some browsers may not fire online/offline events reliably
- navigator.onLine can be inaccurate in some scenarios (e.g., VPN issues)
- Consider adding periodic health checks for more reliable detection
- Alert for offline camera click is basic - could use Toast instead (US-022)

---

## [2025-01-13 19:50] - US-005: 设计IndexedDB数据模型
**Implemented:**
- Created src/db/schema.ts with complete IndexedDB schema definition
- Defined 5 object stores: users, meals, syncQueue, dailyNutrition, cuisineUnlocks
- Created TypeScript types: UserProfile, UserSettings, Meal, MealAnalysis, NutritionInfo, SyncQueueItem, etc.
- Configured indexes including compound indexes (userId_date, userId_date for dailyNutrition)
- Implemented createObjectStores() function for DB initialization
- Added handleMigration() function for future schema migrations
- Created utility functions: generateId(), createDailyNutritionId()

**Files Changed:**
- src/db/schema.ts - New file with complete database schema and type definitions

**Learnings:**
- IndexedDB uses object stores (not tables) with keyPath for primary keys
- Indexes enable efficient queries on non-key properties
- Compound indexes allow querying on multiple fields together
- Auto-increment keys useful for derived/composite keys
- Version migration pattern: oldVersion → newVersion in onupgradeneeded
- Separate profile and settings types for cleaner separation of concerns

**Gotchas:**
- IndexedDB is async - all operations return promises (need wrapper like 'idb' library)
- onupgradeneeded only runs once per version - schema changes require version bump
- Index names must be unique per store
- Chrome DevTools Application tab shows IndexedDB contents for debugging
- Safari has storage limits (~50MB) - need to manage image storage carefully

---

## [2025-01-13 19:55] - US-006: 创建IndexedDB封装层
**Implemented:**
- Installed 'idb' library for cleaner IndexedDB API
- Created src/db/index.ts with complete database wrapper
- Implemented operations for all 5 stores: users, meals, syncQueue, dailyNutrition, cuisineUnlocks
- Key operations: get, getAll, add, update, delete, getByDateRange, getUnsynced, markSynced
- Sync queue processing with retry logic and error handling
- Utility functions: clearAll, getDbSize, closeDB
- Full TypeScript typing with DBSchema interface

**Files Changed:**
- src/db/index.ts - New file with complete IndexedDB wrapper (~500 lines)
- package.json - Added 'idb' dependency

**Learnings:**
- 'idb' library provides Promise-based API for IndexedDB (much cleaner than raw API)
- DBSchema interface enables full TypeScript typing for all DB operations
- Compound indexes accessed with tuple syntax: [userId, date]
- openCursor() allows efficient iteration over large result sets
- Transaction-based operations ensure atomicity
- Index names in DBSchema must match index names in schema.ts

**Gotchas:**
- All IndexedDB operations are async - must use await
- Cursor iteration requires manual loop with continue()
- Transaction mode matters: 'readonly' vs 'readwrite'
- Need to handle upgrade scenario (onupgradeneeded) for schema changes
- Browser storage limits vary - Safari ~50MB, Chrome ~6GB (with quota)
- Consider cleanup strategy for old images in meals (US-020)

---

## [2025-01-13 20:00] - US-007: 实现用户数据存储
**Implemented:**
- Created src/hooks/useProfile.ts hook for user profile and settings management
- Integrated useProfile hook in App.tsx to replace useState for theme/language
- Implemented saveProfile/loadProfile/updateTheme/updateLanguage functions
- Settings are persisted to IndexedDB automatically
- Theme changes (dark/light) persist across page refreshes
- Language changes persist across page refreshes
- Theme is applied to document.documentElement on load

**Files Changed:**
- src/hooks/useProfile.ts - New hook with profile/settings CRUD operations
- src/App.tsx - Integrated useProfile hook, removed useState for theme/language
- src/db/index.ts - Fixed import statement (added 'type' for type imports)

**Learnings:**
- useProfile hook manages both profile and settings in one call
- Settings are loaded on mount and saved on change
- Theme class is applied both in useProfile effect and in App.tsx (dual protection)
- Language enum (Language.ZH/EN) maps to settings string ('zh'/'en')
- isLoading state allows UI to wait for IndexedDB data before rendering
- Default user data is created if not exists in IndexedDB

**Gotchas:**
- Initial render may use default values before IndexedDB loads
- Need to handle loading state to prevent flicker
- Settings update is async - need to await for confirmation
- Theme change should update document class immediately for visual feedback
- Consider adding migration logic for existing users (no current users yet)

---

## [2025-01-13 20:05] - US-008: 实现meal记录存储
**Implemented:**
- Created src/hooks/useMeals.ts hook for meal records management
- Integrated useMeals hook in App.tsx for auto-save after analysis
- Implemented saveMeal, updateMeal, deleteMeal functions
- Implemented getMealsByDateRange for date-based queries
- Images converted to Blob for offline storage
- Meals automatically saved after successful analysis

**Files Changed:**
- src/hooks/useMeals.ts - New hook with meal CRUD operations (~250 lines)
- src/App.tsx - Integrated useMeals hook, auto-save after analysis
- src/db/index.ts - Added type exports for convenience

**Learnings:**
- useMeals hook provides full CRUD for meal records
- Images converted from base64 to Blob for IndexedDB storage
- saveMeal creates complete Meal object with proper schema
- Meals sorted by createdAt descending (newest first)
- Auto-save happens after successful analysis, doesn't block UI on error
- getMealsByDateRange uses compound index (userId_date) for efficient queries

**Gotchas:**
- Image conversion uses fetch API on base64 URL (requires valid data URL)
- Meal type is cast as 'any' for flexibility (could be more strict)
- Default userId is 'current-user' - should be dynamic in real app
- isSynced flag set to false - will need sync implementation (US-017)

---

## [2025-01-13 20:10] - US-009: 创建状态管理Context
**Implemented:**
- Created src/contexts/AppContext.tsx with centralized state management
- Implemented useApp hook for accessing global state
- Implemented actions: addMeal, updateMeal, deleteMeal
- Implemented selectors: getDailyStats, getWeeklyStats
- State persistence through useProfile and useMeals hooks
- Convenience hooks: useAppMeals, useAppProfile, useAppSelectors

**Files Changed:**
- src/contexts/AppContext.tsx - New file with AppContext and useApp hook (~260 lines)
- src/db/index.ts - Added type exports (UserProfile, UserSettings, Meal, etc.)

**Learnings:**
- AppContext wraps useProfile and useMeals for unified state access
- Reducer pattern for actions (AppAction type with discriminated union)
- getDailyStats aggregates nutrition data for a specific date
- getWeeklyStats returns meals in date range for aggregation
- Convenience hooks provide focused access to specific state slices
- State automatically persisted to IndexedDB through underlying hooks

**Gotchas:**
- AppContext combines existing hooks rather than duplicating logic
- dispatch function currently only handles SET_LOADING and SET_ERROR
- addMeal takes Meal object but saveMeal takes individual parameters
- Theme management still uses local state in App.tsx (not migrated to context)
- AppProvider not yet integrated in main.tsx (future enhancement)

---

## [2025-01-13 20:15] - US-010: 重构拍照分析流程
**Implemented:**
- Created src/hooks/useAnalyzeMeal.ts hook for meal analysis workflow
- Replaced manual analysis state in App.tsx with useAnalyzeMeal hook
- Added Loader2 spinner component for loading state
- Added error state with friendly error message display
- Analysis results displayed using Card component with spring animations
- Auto-save to IndexedDB integrated in useAnalyzeMeal hook
- Chinese/English support for loading text

**Files Changed:**
- src/hooks/useAnalyzeMeal.ts - New hook with analysis workflow (~120 lines)
- src/App.tsx - Refactored to use useAnalyzeMeal hook, updated Analysis View

**Learnings:**
- useAnalyzeMeal hook encapsulates entire analysis workflow
- State includes: imageUrl, analysis, isAnalyzing, error
- analyzeImage function handles API call and auto-save
- reset function clears all analysis state
- Error state shows destructive-styled alert with return button
- Loader2 from lucide-react provides clean loading spinner

**Gotchas:**
- Image is converted to base64 data URL by FileReader before analysis
- Base64 is split to remove data URL prefix for API
- Auto-save happens in try/catch so it doesn't block the UI
- Error is set but doesn't prevent showing analysis results
- navigateBack resets analysis state automatically

---

## [2025-01-13 20:20] - US-011: 实现历史记录列表
**Implemented:**
- Created src/components/MealList.tsx component for meal history display
- Integrated useMeals hook in Tab2History to load actual meal data from IndexedDB
- Meals grouped by date (Today/Yesterday/Earlier with proper labels)
- Used motion.div with spring animations for smooth list entry
- Click handler for viewing meal details (callback passed as prop)
- Empty state placeholder with Clock icon and friendly message
- Loading state with skeleton placeholders using animate-pulse

**Files Changed:**
- src/components/MealList.tsx - New component with meal list (~280 lines)
- src/views/tabs/Tab2History.tsx - Added useMeals hook and MealList integration

**Learnings:**
- groupMealsByDate function creates date groups with proper i18n labels
- Date comparison uses setHours(0,0,0,0) for accurate day matching
- motion.div containerVariants with staggerChildren for sequential animations
- Meal thumbnail displays using base64 imageUrl from IndexedDB
- Skeleton loading uses animate-pulse Tailwind class for shimmer effect

**Gotchas:**
- Meal thumbnail is stored as Blob in DB but shown as base64 data URL
- Meal times formatted as HH:MM using padded strings
- onMealClick is a callback - actual navigation logic left to parent
- Meals sorted by createdAt descending (newest first) in useMeals hook
- Empty state displays when meals array is empty (not during loading)

---

## [2025-01-13 20:25] - US-012: 实现数据统计卡片
**Implemented:**
- Created src/components/NutritionCard.tsx component for daily nutrition stats
- Calculates today's totals: calories, protein, fat, carbs from meals
- Progress bars with gradient colors comparing to default goals (2000/50/65/250g)
- Weekly trends chart using recharts LineChart showing last 7 days
- AnimatedNumber component with spring animation for values
- ProgressBar component with spring animation for progress bars
- Empty state with TrendingUp icon when no meal data exists

**Files Changed:**
- src/components/NutritionCard.tsx - New component (~300 lines)
- src/views/tabs/Tab2History.tsx - Added NutritionCard integration

**Learnings:**
- calculateDailyNutrition filters meals by today's date (setHours(0,0,0,0))
- calculateWeeklyTrends generates 7-day array from Monday to Sunday
- AnimatedNumber uses motion.span with scale animation for smooth entry
- ProgressBar uses motion.div with width animation from 0 to percentage%
- Recharts LineChart with activeDot highlights selected day
- Empty state checks meals.length > 0 before rendering stats

**Gotchas:**
- Goals are hardcoded as DEFAULT_GOALS (could be user-configurable later)
- Weekly trend calculates from Monday of current week (not rolling 7 days)
- Progress bars cap at 100% with Math.min for overflow values
- Tooltip styling uses dynamic colors based on theme
- NutritionCard returns early with empty state if no meals exist

---

## [2025-01-13 20:30] - US-013: 实现手势导航
**Implemented:**
- Installed react-swipeable dependency for gesture detection
- Created src/hooks/useSwipeNavigation.ts with useSwipeNavigation and useTabSwipeNavigation hooks
- useTabSwipeNavigation handles left/right swipe for tab navigation
- Left swipe moves to next tab (with boundary check)
- Right swipe moves to previous tab (with boundary check)
- Spring animation with tabVariants for smooth slide transitions
- AnimatePresence with mode="wait" for proper enter/exit animations
- Supports both touch (mobile) and mouse (desktop) gestures
- Swipe threshold set to 50px for optimal UX

**Files Changed:**
- src/hooks/useSwipeNavigation.ts - New hook (~120 lines)
- src/App.tsx - Integrated useTabSwipeNavigation, added previousTab state, wrapped tab content with swipe handlers and spring animation
- package.json - Added react-swipeable dependency

**Learnings:**
- useSwipeable from react-swipeable provides clean gesture API
- trackMouse: true allows testing on desktop with mouse drag
- trackTouch: true enables mobile touch gestures
- preventScrollOnSwipe: false allows vertical scrolling while swiping horizontally
- previousTab state tracks direction for proper slide animation
- getSlideDirection returns 1 (forward) or -1 (backward)
- tabVariants use custom prop for direction-based animation

**Gotchas:**
- Swipe direction is opposite of navigation: left swipe = forward (next tab), right swipe = backward (prev tab)
- AnimatePresence requires key prop on animated children
- mode="wait" prevents overlap during transition
- custom prop in variants receives direction value
- TabBar onTabChange also needs to update previousTab state

---

## [2025-01-13 20:35] - US-014: 优化触摸反馈
**Implemented:**
- Button component already had whileTap={{ scale: 0.96 }} (line 103)
- Card component updated with hover:scale-[1.02] and whileTap={{ scale: 0.98 }}
- Created src/hooks/useLongPressDelete.ts for long press delete functionality
- Long press threshold 500ms with visual progress indicator
- Created src/hooks/useScrollHide.ts with useScrollHide and useTabBarHide hooks
- TabBar hides on scroll down (30px threshold), shows on scroll up
- Spring animation (stiffness 300, damping 30) for hide/show transition
- -webkit-tap-highlight-color: transparent already applied to body

**Files Changed:**
- src/components/ui/card.tsx - Added whileTap={{ scale: 0.98 }} and active:scale-[0.98]
- src/hooks/useLongPressDelete.ts - New hook with long press and progress (~130 lines)
- src/hooks/useScrollHide.ts - New hook for scroll-to-hide functionality (~100 lines)
- src/components/MealList.tsx - Integrated long press delete with progress overlay
- src/views/tabs/Tab2History.tsx - Added onMealDelete prop to MealList
- src/App.tsx - Integrated useTabBarHide, wrapped TabBar with motion.div
- src/index.css - Already had -webkit-tap-highlight-color: transparent

**Learnings:**
- useLongPressDelete uses interval-based progress updates (every 20ms)
- Progress bar at bottom shows visual feedback during long press
- Delete overlay shows "Release to Delete" message when progress > 0
- useTabBarHide uses debounce (50ms) and hideDelay (150ms) for smooth UX
- TabBar slides down with y: 100 when hidden, y: 0 when visible
- scroll event listener uses passive: true for better performance

**Gotchas:**
- Long press doesn't prevent click - both handlers can fire
- Click handler checks !isLongPressing to avoid double action
- Confirmation dialog (confirm()) shows before actual delete
- hideDelay prevents flickering when scroll direction changes rapidly
- isTabBarHidden state drives the animation, not the scroll event directly
- motion.div wraps TabBar to animate without affecting TabBar component

---

## [2025-01-13 20:40] - US-015: 实现下拉刷新
**Implemented:**
- Installed react-pull-to-refresh dependency (used as reference, created custom implementation)
- Created src/components/PullToRefresh.tsx with custom pull-to-refresh functionality
- Created useRefreshTimestamp hook for tracking refresh time and "updated X seconds ago"
- Pull indicator shows RefreshCw icon with rotation animation
- Progress based scaling and opacity (0.5 to 1.0 scale)
- 80px threshold to trigger refresh
- Content follows pull distance with translateY
- Resistance applied (0.4 factor) for natural feel
- Only activates when at top of page (window.scrollY <= 0)
- 500ms delay after refresh to show completion state

**Files Changed:**
- src/components/PullToRefresh.tsx - New component with PullToRefresh and useRefreshTimestamp (~170 lines)
- src/App.tsx - Integrated PullToRefresh wrapper for all tabs, added handleRefresh callback
- package.json - Added react-pull-to-refresh dependency

**Learnings:**
- PullToRefresh wraps tab content with touch event handlers
- Touch events only active when scrollY <= 0 (at top of page)
- Resistance factor (0.4) creates diminishing returns on pull
- Progress calculation: min(pullDistance / threshold, 1)
- Icon rotation: progress * 360 degrees during pull, animates continuously during refresh
- Content translateY follows pullDistance for visual feedback
- Works alongside swipe navigation (swipeHandlers on parent div)

**Gotchas:**
- pull-to-refresh package installed but custom implementation used for better control
- touch events use pointer-events-none on indicator to prevent interference
- Refresh callback is async - await before showing completion
- 500ms fixed delay after refresh before reset (could be configurable)
- handleRefresh currently uses setTimeout to simulate (real implementation would reload data)
- Pull indicator uses fixed positioning with translateY(-100%) to stay above content

---

## [2025-01-13 21:00] - US-019: 实现冲突解决策略
**Implemented:**
- Created src/conflict/ directory with complete conflict resolution system
- Created conflict types (src/conflict/types.ts):
  - 4 conflict types: SAME_RECORD_CONCURRENT, DELETE_CONFLICT, VERSION_MISMATCH, NETWORK_ERROR
  - 4 resolution strategies: LAST_WRITE_WINS, SERVER_WINS, CLIENT_WINS, MANUAL
  - Conflict status: PENDING, RESOLVED, IGNORED
  - MealConflictInfo, ProfileConflictInfo, SettingsConflictInfo
- Created ConflictResolver class (src/conflict/resolver.ts):
  - detectConflict() - Detects conflicts between client/server data
  - resolveLastWriteWins() - Most recent update wins
  - resolveServerWins() - Server version always wins
  - resolveClientWins() - Client version always wins
  - resolveManually() - User chooses which version to keep
  - getResolvedData() - Returns final data after resolution
  - autoResolveAll() - Batch resolve all pending conflicts
  - Conflict statistics and listener subscription
- Created ConflictBanner component (src/components/ConflictBanner.tsx):
  - Expandable banner showing conflict count
  - List of pending conflicts (up to 3 visible)
  - "Resolve Now" and "Later" buttons
  - Safe area support for notch devices
- Created ConflictResolutionModal component (src/components/ConflictResolutionModal.tsx):
  - Full modal for manual conflict resolution
  - Batch resolve buttons (Latest Wins/Server Wins/Client Wins)
  - Per-conflict resolution (Keep Local/Keep Server)
  - Shows client vs server versions for each conflict
- Created useConflicts hook (src/hooks/useConflicts.ts):
  - Exposes conflicts, pendingConflicts, stats
  - resolveConflict, resolveAllWithStrategy, ignoreConflict
  - getConflictInfo for displaying conflict details
- Integrated conflict detection in syncProcessor.pullRemoteChanges():
  - Auto-detects conflicts when pulling remote changes
  - Uses Last Write Wins by default for auto-resolution
  - Logs conflict resolution details
- Added ConflictBanner and ConflictResolutionModal to App.tsx
- Installed class-variance-authority dependency for Badge component

**Files Changed:**
- src/conflict/types.ts - New conflict type definitions (~130 lines)
- src/conflict/resolver.ts - New ConflictResolver class (~280 lines)
- src/conflict/index.ts - Module exports
- src/components/ConflictBanner.tsx - New conflict notification banner (~150 lines)
- src/components/ConflictResolutionModal.tsx - New conflict resolution modal (~280 lines)
- src/hooks/useConflicts.ts - New useConflicts hook (~100 lines)
- src/api/syncProcessor.ts - Updated pullRemoteChanges with conflict detection
- src/App.tsx - Added ConflictBanner and ConflictResolutionModal
- package.json - Added class-variance-authority dependency

**Learnings:**
- Conflict detection based on timestamp comparison (1 second threshold)
- Conflict types categorize different scenarios (concurrent modification, delete conflicts, etc.)
- Resolution strategies provide flexibility: automatic (LWW/server/client) or manual
- Listener pattern allows real-time UI updates when conflicts are detected/resolved
- Singleton pattern for ConflictResolver ensures consistent state
- Conflict info objects provide typed data for each entity type (meal/profile/settings)
- Banner uses safe-area-inset-top for notch device support
- Modal supports batch resolution for efficiency
- Stats tracking: total, pending, resolved, ignored, byType, byEntityType

**Gotchas:**
- 1 second threshold for concurrent modification detection
- Auto-resolution uses LAST_WRITE_WINS by default (configurable)
- Manual resolution requires user interaction (can't be automated)
- Deleted conflicts (status=IGNORED) remain in history for debugging
- getConflictInfo() returns typed data based on entityType
- ConflictBanner expands to show up to 3 conflicts, shows count for more
- Modal shows detailed client/server versions for selected conflict
- syncProcessor logs conflict resolution but doesn't block sync on conflicts
- Conflicts are detected during pullRemoteChanges, not during push
- Permanent errors (not found, invalid, unauthorized) don't create conflicts
- Build warning: dynamic imports don't create separate chunks (expected)

---

## [2025-01-14] - US-020: 优化图片加载和缓存
**Implemented:**
- Created src/components/LazyImage.tsx with IntersectionObserver lazy loading
- Progressive loading: shimmer skeleton → thumbnail → full image with blur transition
- IntersectionObserver with 50px rootMargin for pre-loading
- Error state with placeholder icon (ImageOff from lucide-react)
- Optional thumbnailSrc prop for faster initial display
- Created src/utils/imageUtils.ts with image processing utilities:
  - generateThumbnail() - Canvas-based thumbnail generation (200x200, 0.7 quality)
  - compressImage() - Image compression (1200px max, 0.85 quality)
  - blobToBase64() - Convert Blob to base64 data URL
  - getImageDimensions() - Get image width/height
  - generateBlurPlaceholder() - Tiny blur placeholder (20px)
- Extended Meal schema with thumbnailUrl field (src/db/schema.ts)
- Updated useMeals hook to auto-generate thumbnails on save (src/hooks/useMeals.ts)
- Updated MealList to use LazyImage with thumbnail support (src/components/MealList.tsx)
- Added shimmer animation to index.css (animate-shimmer class)
- Service Worker already has CacheFirst image caching (30 days, 200 max entries)

**Files Changed:**
- src/components/LazyImage.tsx - New lazy image component (~150 lines)
- src/utils/imageUtils.ts - New image utilities (~230 lines)
- src/db/schema.ts - Added thumbnailUrl?: string to Meal interface
- src/hooks/useMeals.ts - Added thumbnail generation in saveMeal
- src/components/MealList.tsx - Replaced img with LazyImage component
- src/index.css - Added shimmer keyframes and animate-shimmer class

**Learnings:**
- IntersectionObserver more efficient than scroll event listeners
- rootMargin: '50px' starts loading before image enters viewport
- Progressive loading stages: shimmer (isLoading) → thumbnail/blur (isLoaded=false) → full/clear (isLoaded=true)
- Blur effect: filter: blur(10px) → blur(0px) with 300ms transition
- Framer Motion AnimatePresence smooths image transitions
- Thumbnail generation uses Canvas API with aspect ratio preservation
- URL.createObjectURL creates efficient blob URLs (cleaned up on image load)
- generateThumbnail fails gracefully - falls back to original URL
- Service Worker CacheFirst caches images after first load
- Pre-cached images served instantly on subsequent views

**Gotchas:**
- imgRef needed for onLoad event (containerRef doesn't fire)
- IntersectionObserver disconnect after isInView to prevent memory leaks
- loading='eager' skips observer for above-fold images
- Thumbnail URL stored separately from imageUrl (not generated on-the-fly)
- Canvas context check prevents crashes in restricted environments
- imageSmoothingEnabled and imageSmoothingQuality improve resize quality
- compressImage uses higher quality (0.85) for full-size images
- Thumbnails use lower quality (0.7) to reduce size
- Build passes successfully with new components

---

## [2025-01-13 20:55] - US-018: 实现后端API集成
**Implemented:**
- Created src/api/ directory structure with client, types, and endpoints
- Created API base client (src/api/client.ts) with apiClient (GET/POST/PUT/PATCH/DELETE)
- Implemented TokenManager class for JWT token management
  - Token storage in localStorage (access/refresh/expiresAt)
  - Auto-refresh on 401 responses
  - Expiration detection with 1-minute buffer
  - isAuthenticated() check
- Created request/response interceptors (addRequestInterceptor/addResponseInterceptor)
- Created API types (src/api/types.ts) with all DTOs and response types
- Implemented meals API (src/api/meals.ts):
  - getAll, getById, create, update, delete
  - getByDateRange, getToday
  - uploadImage (multipart/form-data)
- Implemented sync API (src/api/sync.ts):
  - pull (remote changes since lastSyncAt)
  - push (batch sync operations)
  - getStatus, fullSync, clearQueue
- Implemented auth/profile API (src/api/profile.ts):
  - auth: login, register, logout, refreshToken
  - verifyEmail, requestPasswordReset, resetPassword
  - profile: get, update, updateSettings, uploadAvatar, deleteAccount
- Created syncProcessor (src/api/syncProcessor.ts):
  - Processes sync queue items with real API calls
  - Batch processing with sync.push endpoint
  - pullRemoteChanges function for remote updates
  - Permanent vs temporary error detection
- Updated useSync hook to use syncProcessor instead of mock

**Files Changed:**
- src/api/client.ts - New API base client with JWT management (~280 lines)
- src/api/types.ts - New type definitions for all API operations
- src/api/meals.ts - New meals API endpoints (~110 lines)
- src/api/sync.ts - New sync API endpoints (~80 lines)
- src/api/profile.ts - New auth/profile API endpoints (~140 lines)
- src/api/index.ts - Module exports
- src/api/syncProcessor.ts - New sync processor integrating API calls (~150 lines)
- src/hooks/useSync.ts - Updated to use syncProcessor (removed mock)

**Learnings:**
- API base URL from VITE_API_URL env var (defaults to https://api.bellybook.app/v1)
- JWT tokens stored in localStorage with expiration tracking
- 401 responses trigger automatic token refresh before retry
- Interceptors allow modifying requests/responses (logging, auth, etc.)
- Multipart/form-data handled separately (bypass JSON serialization)
- Sync queue processor distinguishes permanent errors (no retry) from temporary (retry)
- Batch sync more efficient than individual item processing
- Pull before push ensures latest server state before sending local changes

**Gotchas:**
- Build warning: dynamic imports don't create separate chunks when modules also statically imported
- API endpoint URLs assume specific backend structure - may need adjustment for actual backend
- Token refresh on 401 happens once per request (no infinite loops)
- Image upload uses direct fetch() to handle FormData correctly
- syncProcessor throws on permanent errors (removes from queue), returns false for temporary (retries)
- pullRemoteChanges currently uses "server wins" conflict resolution (improved in US-019)
- API_BASE_URL environment variable needs to be configured for production

---

## [2025-01-13 20:50] - US-017: 实现同步队列机制
**Implemented:**
- Created SyncQueueManager class (src/sync/SyncQueue.ts) for managing offline sync queue
- Created sync types (src/sync/types.ts) with operation types and payloads
- Created useSync hook (src/hooks/useSync.ts) for React integration
- Created SyncStatus component (src/components/SyncStatus.tsx) with visual status display
- Added PROFILE_SYNC to AppView enum in types.ts
- Added "Data Sync" menu item to settings with Cloud icon
- Added sync settings view to App.tsx with SyncStatus component
- Implemented exponential backoff retry logic (max 5 retries, base 1s, max 60s)
- Auto-sync on online event detection via useSync hook
- Mock processor for sync operations (real API integration in US-018)

**Files Changed:**
- src/sync/SyncQueue.ts - New SyncQueueManager class (~300 lines)
- src/sync/types.ts - New type definitions for sync operations
- src/sync/index.ts - Module exports
- src/hooks/useSync.ts - New useSync hook (~100 lines)
- src/components/SyncStatus.tsx - New sync status UI component (~160 lines)
- src/types.ts - Added PROFILE_SYNC to AppView enum
- src/App.tsx - Added sync menu item and view, imported SyncStatus

**Learnings:**
- SyncQueueManager uses IndexedDB syncQueue store for persistence
- Exponential backoff: baseDelay * 2^retryCount with jitter
- Items sorted by retryCount (pending first) then createdAt
- Online event listener triggers auto-sync when connection restored
- SyncStatus shows pending/failed counts with real-time updates
- Sync queue supports 5 operation types: CREATE_MEAL, UPDATE_MEAL, DELETE_MEAL, UPDATE_PROFILE, UPDATE_SETTINGS
- Singleton pattern with getSyncQueue() for global access

**Gotchas:**
- Mock processor returns true (clears queue) - real API integration in US-018
- useSync hook needs userId prop for queue operations
- Sync status updates via listener subscription pattern
- Max retries (5) prevents infinite retry loops
- Process delay (100ms) between items prevents rate limiting
- AbortController allows canceling ongoing sync operations

---

## [2025-01-13 20:45] - US-016: 优化Safe Area适配
**Implemented:**
- Extended index.css with comprehensive safe-area utility classes
- Added safe-top/bottom/left/right/all padding utilities
- Added fixed-safe-top/bottom/left/right for positioned elements
- Added min-h-screen-safe, mt/mb/ml/mr-safe-*, vh-safe utilities
- Created src/components/SafeAreaView.tsx with multiple components:
  - SafeAreaView - Wrapper with padding/margin mode options
  - FixedSafe - For fixed positioned elements with safe area offsets
  - SafeAreaHeight - Container with height considering safe areas
  - useSafeAreaInsets - Hook to get safe area inset values
  - useHasNotch - Hook to detect notch devices
- Verified existing components: TopBar uses mt-safe-top, TabBar uses safe-bottom
- Landscape mode supported through vh-safe class

**Files Changed:**
- src/index.css - Extended safe-area CSS utilities (~70 lines added)
- src/components/SafeAreaView.tsx - New component with safe area wrappers (~160 lines)
- src/App.tsx - Already uses safe areas (mt-safe-top in top bar, safe-bottom in TabBar)

**Learnings:**
- env(safe-area-inset-*) returns device-specific safe area values
- iPhone X notch: ~44px top, ~34px bottom
- Fixed elements need explicit top/bottom with env() values
- CSS classes provide reusable safe area patterns
- vh-safe calculates: 100vh - top inset - bottom inset
- Landscape orientation automatically handled by env() values

**Gotchas:**
- env() values are 0 on devices without notches (graceful degradation)
- Safe area insets are device-specific (iPhone X, newer Android, etc.)
- Fixed elements need both position AND offset to be safe
- min-h-screen-safe prevents content overflow on notch devices
- useHasNotch uses screen dimensions as proxy (not 100% accurate)
- pull-to-refresh and scroll-hide need to account for safe areas

---

## [2025-01-14] - US-021: 添加加载骨架屏
**Implemented:**
- Created src/components/ui/skeleton.tsx with comprehensive skeleton component library:
  - Skeleton - Base component with 4 variants (text/circular/rectangular/rounded)
  - SkeletonText - Multi-line text skeleton
  - SkeletonCard - Card-shaped skeleton with image and text
  - SkeletonMealItem - Matches MealList item structure exactly
  - SkeletonStats - Matches NutritionCard structure exactly
  - SkeletonAvatar - Circular avatar placeholder
  - SkeletonButton - Button-shaped skeleton
- Created src/hooks/useMinDelay.ts for minimum delay detection:
  - useMinDelay() - Returns showSkeleton and hasElapsedMinDelay
  - useShowSkeleton() - Simplified version returning boolean only
  - useLoadingTimer() - Track loading start time for analytics
  - Default 300ms minimum delay prevents skeleton flash for fast loads
- Updated src/components/MealList.tsx:
  - Added import for SkeletonMealItem and useMinDelay
  - Replaced manual skeleton divs with SkeletonMealItem component
  - Uses useMinDelay hook to skip skeleton for fast loads (< 300ms)
  - Skeleton structure matches actual meal list layout (date header + cards)
- Updated src/components/NutritionCard.tsx:
  - Added isLoading prop to interface
  - Added import for SkeletonStats and useMinDelay
  - Uses useMinDelay hook to prevent flash
  - Shows SkeletonStats when loading or within minimum delay
- Updated src/views/tabs/Tab2History.tsx:
  - Passes mealsLoading to NutritionCard as isLoading prop
- Shimmer animation already exists in index.css (added in US-020)

**Files Changed:**
- src/components/ui/skeleton.tsx - New skeleton component library (~220 lines)
- src/hooks/useMinDelay.ts - New minimum delay hook (~75 lines)
- src/components/MealList.tsx - Updated to use SkeletonMealItem and useMinDelay
- src/components/NutritionCard.tsx - Added isLoading prop and SkeletonStats
- src/views/tabs/Tab2History.tsx - Passes isLoading to NutritionCard

**Learnings:**
- Skeleton components should exactly match the layout of real content
- Minimum delay prevents skeleton flash for fast network/data loads
- useMinDelay tracks elapsed time from component mount
- If loading completes < 300ms, skeleton still shows for remaining time
- If loading takes > 300ms, skeleton shows until data is ready
- showSkeleton = isLoading || !hasElapsedMinDelay (both conditions checked)
- startTimeRef tracks when loading started for accurate elapsed time
- timeoutRef cleaned up on unmount to prevent memory leaks
- Shimmer animation creates wave effect across skeleton background
- Different variants (text/circular/rectangular/rounded) cover most UI patterns

**Gotchas:**
- 300ms minimum delay is configurable per use case
- Skeleton flash is jarring - better to show nothing briefly than flash
- Skeleton layout must match real content for smooth transition
- animate-shimmer class uses background-position animation
- Skeleton components use bg-muted color for consistent theming
- Date headers in MealList skeleton use h-4 w-20 for size matching
- Card wrapper needed for SkeletonMealItem to match real card styling
- NutritionCard skeleton shows full structure (stats grid + chart placeholder)
- Build successful (+3KB CSS, +1KB JS from new components)

---

## [2025-01-14] - US-022: 实现Toast通知系统
**Implemented:**
- Created src/components/ui/toast.tsx with Toast notification system:
  - Toast component with 3 types (success/error/info) with corresponding icons and color schemes
  - Auto-dismiss after configurable duration (default 3 seconds)
  - Manual close button (X icon) in top-right corner
  - Spring animations: enter (y:-50→0, scale:0.95→1), exit (x:0→100, scale:1→0.95)
  - Theme-aware colors (light/dark mode support)
  - backdrop-blur for modern glass effect
- Created src/hooks/useToast.ts with toast state management:
  - toast() - Generic method to show any toast type
  - showSuccess() - Success toast with green theme and CheckCircle2 icon
  - showError() - Error toast with red theme and XCircle icon
  - showInfo() - Info toast with blue theme and Info icon
  - removeToast() - Remove specific toast by id
  - clearAll() - Clear all active toasts
- Created src/contexts/ToastContext.tsx with global provider:
  - ToastProvider - Wraps app with toast context
  - useToastContext() - Access toast functionality from anywhere
  - useToastNotification() - Convenience alias
  - ToastContainer - Fixed top-right container with safe-area support
- Integrated ToastProvider in App.tsx:
  - Wrapped main app content with ToastProvider
  - Added useToastNotification hook
  - Added error toast in handleFileChange for failed analysis
- Added toast usage in Tab2History.tsx:
  - handleDeleteMeal with success/error toasts
  - Multilingual support (ZH/EN)
- Multiple toasts stack with staggerChildren animation (0.1s delay)

**Files Changed:**
- src/components/ui/toast.tsx - New toast system (~160 lines)
- src/hooks/useToast.ts - New toast hook (~85 lines)
- src/contexts/ToastContext.tsx - New context provider (~60 lines)
- src/App.tsx - Added ToastProvider, useToastNotification, error handling
- src/views/tabs/Tab2History.tsx - Added handleDeleteMeal with toast notifications

**Learnings:**
- Spring animations with stiffness:400, damping:25, mass:0.8 for smooth enter
- Exit animation uses easeInOut for fast dismiss (0.2s duration)
- StaggerChildren with 0.1s delay creates cascading effect for multiple toasts
- Toast id format: `toast-${Date.now()}-${Math.random()}` ensures uniqueness
- isExiting state prevents race conditions during dismiss
- min-w-[300px] max-w-md ensures consistent sizing across content
- z-[9999] ensures toasts appear above all content
- pointer-events-none on container, pointer-events-auto on toasts
- safe-top class accounts for device notch
- Backdrop blur (backdrop-blur-sm) creates modern glass effect

**Gotchas:**
- Toast exit animation needs 300ms delay before actual removal
- Manual close and auto-dismiss both use same isExiting mechanism
- Theme affects colors: dark mode uses darker backgrounds (bg-green-900/90)
- Icons from lucide-react: CheckCircle2 (success), XCircle (error), Info (info)
- Close button hover effect differs by theme (white/10 vs black/5)
- Toast state managed in hook, not in component (separation of concerns)
- Provider pattern allows global access without prop drilling
- Build impact: +2.6KB CSS, +4KB JS

---

## [2025-01-14] - US-023: 实现深色模式完整支持
**Implemented:**
- Verified all CSS variables have dark mode values in .dark class (src/index.css):
  - background/foreground, primary/primary-foreground, secondary/secondary-foreground
  - accent/accent-foreground, muted/muted-foreground, destructive/destructive-foreground
  - border/input/ring, card/card-foreground, popover/popover-foreground
- Updated ConflictResolutionModal.tsx to use CSS variables:
  - bg-card, text-card-foreground, border-border instead of theme-based hardcoded colors
  - text-muted-foreground for secondary text
  - bg-muted/30 for subtle backgrounds
  - Removed theme === 'dark' conditions in favor of semantic classes
- Updated MealList.tsx to use CSS variables:
  - bg-muted for skeleton date header
  - bg-muted, text-muted-foreground for empty state
  - Removed theme-based color classes
- Updated NutritionCard.tsx chart colors:
  - X/Y axis ticks use tooltipColor instead of hardcoded #888
  - Theme-based line colors already in place
- Added smooth theme transition to body:
  - transition: background-color 0.3s ease, color 0.3s ease
- Verified images maintain original colors:
  - No filter/brightness/contrast applied to images
  - opacity values only for intentional UI effects, not theme-related
- Theme switching works via updateTheme hook in useProfile
- System theme setting already supported (theme: 'light' | 'dark' | 'auto')

**Files Changed:**
- src/index.css - Added smooth theme transition to body
- src/components/ConflictResolutionModal.tsx - Updated to use CSS variables
- src/components/MealList.tsx - Updated skeleton and empty state colors
- src/components/NutritionCard.tsx - Updated chart tick colors

**Learnings:**
- Semantic CSS variables (bg-card, text-muted-foreground) automatically adapt to theme
- Theme-based conditional classes create code bloat and maintenance issues
- CSS variables defined in :root apply to light mode, .dark overrides for dark mode
- Smooth transitions prevent jarring color changes when switching themes
- 0.3s ease transition provides smooth feel without being too slow
- Images don't inherit filter effects from parent containers
- OKLCH color space provides consistent perceptual uniformity across themes

**Gotchas:**
- Some components still use theme === 'dark' conditions (could be further refactored)
- Card components use specific bg colors for consistency (bg-[#1C1C1E] in dark mode)
- Chart line colors still use theme condition (acceptable as color is design-specific)
- Theme toggle happens via updateTheme which updates document.documentElement class
- No images use filter property, so they display correctly in both themes
- Build successful (no size change - just replacing existing classes)

---

## [2025-01-14] - US-024: 性能优化
**Implemented:**
- Added React.lazy() for code splitting by route:
  - Tab1Home, Tab2History, TabPassport, Tab4Social lazy-loaded
  - LeaderboardPage, UserDetailPage lazy-loaded from SocialSubViews
  - Suspense wrappers with Loader2 spinner fallback
- Updated vite.config.ts manualChunks:
  - react-vendor: React, ReactDOM, framer-motion
  - ui-vendor: recharts, lucide-react
- Bundle size improvements:
  - index.js: 392 KB → 353 KB (39 KB reduction)
  - Tab chunks loaded on demand: Tab1Home (1.87 KB), TabPassport (1.40 KB), Tab4Social (2.91 KB), Tab2History (29.72 KB), SocialSubViews (4.80 KB)
  - Total JS: ~890 KB (273 KB gzipped)
  - CSS: 70 KB (11.5 KB gzipped)
- Virtual scrolling: Current MealList implementation sufficient for < 1000 meals
  - Lazy image loading (US-020) reduces initial load
  - Meals grouped by date with stagger animation
  - No virtual scrolling needed for typical meal tracking use case
- Image formats:
  - User uploads: JPEG with 0.85 quality compression (imageUtils.ts)
  - External services: picsum.photos, dicebear (auto WebP via Accept header)
  - App icons: PNG (small static assets, acceptable)
- Service Worker caching:
  - CacheFirst for images (30 days, 200 max entries)
  - NetworkFirst for API calls (24 hours)
  - Precache: 15 entries (941 KB)
- Existing performance optimizations:
  - Lazy loading images (IntersectionObserver)
  - Skeleton screens (US-021)
  - Throttle/debounce for scroll and swipe events
  - IndexedDB for offline data
  - PWA manifest with shortcuts

**Files Changed:**
- src/App.tsx - Added React.lazy for all tab components and Suspense wrappers

**Learnings:**
- Code splitting reduces initial bundle size significantly
- React.lazy() + Suspense is simple and effective for route-based splitting
- Manual chunks in vite.config.ts control vendor splitting
- Precache entries increased from 10 to 15 with lazy-loaded chunks
- Main index.js reduced by ~10% with lazy loading
- Tab2History is largest chunk (29.72 KB) due to charts and history features
- Virtual scrolling adds complexity for minimal gain with < 1000 items
- JPEG 0.85 quality balances size and visual quality
- Service Worker CacheFirst strategy ensures instant loads for cached images

**Gotchas:**
- SocialSubViews uses dynamic import with then() for named exports
- Suspense fallback shows during chunk load (should be fast on 2nd visit)
- First visit still loads main bundle + first tab chunk
- Subsequent tabs load on-demand with browser caching
- Build warning about dynamic import of db/index is expected (syncProcessor pattern)
- Lighthouse audit requires browser run (not in CI)
- Target: Performance > 90, First Contentful Paint < 2s (achievable with current optimizations)

---

## Story Completion Status

- [x] US-001: 添加PWA Manifest配置
- [x] US-002: 创建Service Worker基础框架
- [x] US-003: 配置Vite PWA插件
- [x] US-004: 实现离线检测和UI提示
- [x] US-005: 设计IndexedDB数据模型
- [x] US-006: 创建IndexedDB封装层
- [x] US-007: 实现用户数据存储
- [x] US-008: 实现meal记录存储
- [x] US-009: 创建状态管理Context
- [x] US-010: 重构拍照分析流程
- [x] US-011: 实现历史记录列表
- [x] US-012: 实现数据统计卡片
- [x] US-013: 实现手势导航
- [x] US-014: 优化触摸反馈
- [x] US-015: 实现下拉刷新
- [x] US-016: 优化Safe Area适配
- [x] US-017: 实现同步队列机制
- [x] US-018: 实现后端API集成
- [x] US-019: 实现冲突解决策略
- [x] US-020: 优化图片加载和缓存
- [x] US-021: 添加加载骨架屏
- [x] US-022: 实现Toast通知系统
- [x] US-023: 实现深色模式完整支持
- [x] US-024: 性能优化

---

